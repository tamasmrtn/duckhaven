# Dialect differences

One row per query that needed a change from the canonical TPC-H text
(`queries/tpch_canonical/`) to run on a given engine. Generated by
`render_dialects.py` from its `OVERRIDES` table — do not hand-edit.

| Engine | Query | Change | Reason |
|---|---|---|---|
| snowflake | Q22 | `substring(c_phone FROM 1 FOR 2)` -> `SUBSTR(c_phone, 1, 2)` | Snowflake rejects SQL-standard SUBSTRING(x FROM .. FOR ..); confirmed via a live 001003 syntax error, Phase 0 SF1 dry run. |
