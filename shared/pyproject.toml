[project]
name = "duckhaven-shared"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pydantic>=2.0", "opentelemetry-api>=1.36"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/duckhaven_shared"]
