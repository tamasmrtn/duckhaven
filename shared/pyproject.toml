[project]
name = "duckhaven-shared"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pydantic>=2.13.5", "opentelemetry-api>=1.44.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/duckhaven_shared"]
