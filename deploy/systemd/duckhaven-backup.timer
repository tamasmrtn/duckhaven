# Runs the backup nightly (G-D18-a). Pairs with duckhaven-backup.service.
[Unit]
Description=Run duckhaven Postgres backup nightly

[Timer]
OnCalendar=*-*-* 02:30:00
Persistent=true

[Install]
WantedBy=timers.target
