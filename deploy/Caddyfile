{
    auto_https off
}

:443 {
    tls internal

    reverse_proxy /api/* api:8000
    reverse_proxy /agents/* api:8000

    # Serve the built SPA — replace with file_server once web is built
    reverse_proxy /* api:8000
}
